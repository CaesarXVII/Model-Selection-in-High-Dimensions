---
title: "Practical2_worked"
author: "Alexander Maslev"
date: "2018 M02 25"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```


## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r iris}
iris_sub=subset(iris,Species=="setosa")
```

## Including Plots

You can also embed plots, for example:

```{r iris_sub,include=FALSE}
hist(iris_sub$Sepal.Width)
```





```{r echo=FALSE}
hist(iris_sub$Sepal.Width)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.


P(Species="virginica"|Sepal.Width>3)=P(Species="virginica" n Sepal.Width>3)/P(Sepal.Width>3)

$\Pr( Species="setosa" \mid Sepal.Width>3 )= \frac {Pr( Species="setosa" \cap  Sepal.Width>3)} {Pr(Sepal.Width>3)}$

```{r}
union_p=nrow(subset(iris,iris[,5]=="setosa" & iris[,2]>3))
sepal_p=nrow(subset(iris,iris[,2]>3))
cond_prob=union_p/sepal_p
cond_prob
```


